#include <stdio.h>

int main()
{
    char ch;
    int num;
    float fnum;
    double dnum;
    short int snum;
    long int lnum;
    long long int llnum;
    printf("Size of char (ch) = %d \n", sizeof(char));                      // sizeof(ch)
    printf("Size of int (num) = %d \n", sizeof(num));                       // sizeof(ch)

}